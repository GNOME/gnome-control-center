using Gtk 4.0;
using Adw 1;

template $CcBoltDeviceEntry: Adw.ActionRow {
  activatable: true;

  [suffix]
  Image status_warning {
    visible: false;
    icon-name: "dialog-warning-symbolic";
    icon-size: normal;
  }
}
